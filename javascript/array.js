const fruits = ["Banana","Apple"];
 const fruitsString = fruits.join(", ");
 console.log(fruitsString);

// Array Example in JavaScript

// // 1. Create Array
// let arr = [10, 20, 30, 40, 50];
// console.log("Original Array:", arr);

// // 2. Traverse (print each element)
// console.log("\nTraverse Array:");
// arr.forEach((value, index) => {
//   console.log(`Index: ${index}, Value: ${value}`);
// });

// // 3. Insert (push at end)
// arr.push(60);
// console.log("\nAfter Push (Insert at End):", arr);

// // Insert at beginning (unshift)
// arr.unshift(5);
// console.log("After Unshift (Insert at Beginning):", arr);

// // 4. Delete (pop and shift)
// arr.pop(); // remove last element
// console.log("\nAfter Pop (Delete from End):", arr);

// arr.shift(); // remove first element
// console.log("After Shift (Delete from Beginning):", arr);

// // 5. Update (change value at index 2)
// arr[2] = 100;
// console.log("\nAfter Update at Index 2:", arr);

// // 6. Search (find an element)
// let searchValue = 40;
// let index = arr.indexOf(searchValue);
// if (index !== -1) {
//   console.log(`\nValue ${searchValue} found at index ${index}`);
// } else {
//   console.log(`\nValue ${searchValue} not found`);
// }

// // 7. Delete specific index (splice)
// arr.splice(1, 1); // remove 1 element at index 1
// console.log("\nAfter Splice (Delete index 1):", arr);

// // 8. Length of array
// console.log("\nLength of Array:", arr.length);
